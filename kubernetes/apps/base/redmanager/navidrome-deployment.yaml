apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome
  namespace: redmanager
  labels:
    app: navidrome
spec:
  replicas: 1
  selector:
    matchLabels:
      app: navidrome
  template:
    metadata:
      labels:
        app: navidrome
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"
      containers:
        - name: navidrome
          image: docker.io/deluan/navidrome:latest@sha256:b14a6acb5cd5ee73f3a13d63d8d68ede82dedb796aa522fbada94769d990cf0b
          env:
            - name: ND_MUSICFOLDER
              value: /music
            - name: ND_DATAFOLDER
              value: /data
            - name: ND_CACHEFOLDER
              value: /tmp-cache
            - name: ND_ENABLEFAVOURITES
              value: "false"
            - name: ND_ENABLESTARRATING
              value: "false"
            - name: ND_ENABLEINSIGHTSCOLLECTOR
              value: "false"
            - name: ND_CONFIGFILE
              value: "/data/navidrome.toml"
            - name: ND_DEVAUTOCREATEADMINPASSWORD
              valueFrom:
                secretKeyRef:
                  name: redmanager-secret
                  key: ND_DEVAUTOCREATEADMINPASSWORD
          securityContext:
            seccompProfile:
              type: RuntimeDefault
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 4533
              name: http
              protocol: TCP
          volumeMounts:
            - name: music
              mountPath: /music
            - name: data
              mountPath: /data
            - name: tmp-cache
              mountPath: /tmp-cache

      volumes:
        - name: music
          persistentVolumeClaim:
            claimName: music
        - name: data
          persistentVolumeClaim:
            claimName: navidrome-data
        - name: tmp-cache
          emptyDir: {}
