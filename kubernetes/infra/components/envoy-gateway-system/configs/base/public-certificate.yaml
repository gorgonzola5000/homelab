apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: public-wildcard-cert
spec:
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - "*.${HOMELAB_DOMAIN}"
  secretName: public-wildcard-tls-secret
