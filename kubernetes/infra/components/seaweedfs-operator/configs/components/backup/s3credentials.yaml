apiVersion: seaweed.seaweedfs.com/v1
kind: S3Credentials
metadata:
  name: backup-s3-credentials
spec:
  seaweedRef:
    name: seaweed
  identityRef:
    name: backup-s3-credentials
  secretRef:
    name: backup-s3-credentials
